Host gitlab.com codeberg.org
    User git
    ProxyCommand nc -v -x "$(cat .proxy-url | sd 'socks5h://' '')" %h %p
Host github.com
    Hostname ssh.github.com
    Port 443
    User git
    ProxyCommand nc -v -x $(cat .proxy-url | sed "s/socks5h:\/\///") %h %p
